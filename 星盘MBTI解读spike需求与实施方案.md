# 星盘 × MBTI 性格解读 — Spike 需求与实施方案

> **实现状态（2026-07-21）**：本地全链路已落地（流式报告、可选「想问的事」五节结构、塔罗翻牌、暗色星空 UI、HTML/PDF 导出）。发群前清单见 [SHIP.md](SHIP.md)。本文为纸面审查 **v1.4 封版**，不再改方案正文；进度见 [history.md](history.md) / [architecture.md](architecture.md) / [README.md](README.md)。

**版本**：v1.4（2026-07-21，最终版：月亮换座处理、朋友样本偏差降权。纸面审查到此为止，下一步是代码） **性质**：可逆实验（spike），不是产品立项 **时间盒**：两个周末，硬上限 **验收标准**（v1.3 修正为两条腿，v1.4 补充信号分级）：定量——发放两周后 LLM API 调用量是否有超出首轮的自发增量；定性——观察期结束后直接询问十位朋友"后来又用过吗、有没有转发给别人"。因隐私承诺不留用户数据，无法从数据上识别个体回访，定性访谈是最终裁决依据。**信号分级**：朋友本人的二次使用 = 弱信号（含人情客气偏差）；朋友自发转发给作者不认识的人且对方使用 = 强信号（转发消耗朋友自己的社交信用，无法用客气解释）。商业化评审以强信号为准。有真实拉力 → 再谈商业化审查；无 → 归档，不追加投入。

**封版声明**：v1.4 为纸面审查的最终版本。此后的问题一律由运行中的代码暴露和解答，不再进行第四轮方案审查——继续打磨文档的边际收益已低于动手，且"完美方案"是动手前最舒适的拖延形态。

---

## 一、项目定位（已锁定的边界）

- 免费、不推广、不收费、不留存用户账号。
- 用户即朋友圈，约 10 人，中文用户，手机点链接使用。
- 本项目**不服务于**当前职业锚点（C++ 工程师工具采用），因此严格限制在业余时间盒内，超时即停，不与主线争夺精力。

---

## 二、五步审查（决策过程存档）

### 1. 需求表

|需求|来源/Owner|结论|理由|
|---|---|---|---|
|输入出生年月日+时间+地点|星盘计算的数学必需项|**保留**|天文计算的硬输入|
|出生时间允许"不知道"|现实约束：相当比例的人不知道生时|**保留（新增）**|否则漏斗在第一步就断，验收指标失真|
|MBTI 输入|你（产品差异化假设）|**放宽**为 16 型下拉框|内置测试题是整个测试流程页面的仓位，spike 不配|
|外接 astro-seek 自动生成星盘|你（原始想法）|**推翻**|无公开 API，抓取脆弱且不合规；星盘是纯天文数学，开源库可完全替代|
|GPT 生成星盘图|你（讨论中提出）|**推翻**|生成式图像画不了精密图表，行星位置必然是幻觉，可信度归零|
|星盘可视化图|用户体验（报告观感）|**保留**|kerykeion 自带 SVG 出图，成本≈0，不用白不用|
|数据发给 ChatGPT 做解读|你（核心价值假设）|**保留**，泛化为"LLM API"|这是 spike 要验证的核心：AI 解读是否有重复使用的拉力|
|一次性报告（非对话）|你（已确认）|**保留**|决定了无账号、无历史、单页结果的极简架构|
|塔罗抽牌+结合星盘解读|**朋友的主动请求**（产品前口头表态，弱佐证但真实）|**保留（v1.2 新增）**，限定为报告页底部的可选第二步|不进主表单，不污染核心漏斗；半天仓位，可逆|

### 2. 删除清单

- **删除 astro-seek 依赖**（整个外部抓取环节，项目最大风险源）
- **删除 GPT 生图**
- **删除内置 MBTI 测试题库与计分逻辑**
- **删除账号系统、登录、历史记录、数据库**（一次性报告无状态）
- **删除自定义前端**（React/Vercel 路线，与壁垒方向不同向）
- 观察名单（未来可能加回）：星盘图美化、对话式追问、英文版

### 3. 简化

- 前端+后端+部署 三套系统 → **Streamlit 单文件应用**，一套 Python 到底
- 星盘计算+出图 两个环节 → **kerykeion 一个库**同时解决
- 出生地经纬度换算 → kerykeion 内置 GeoNames 查询（免费注册一个 GeoNames 用户名即可），不自建地理数据库

### 4. 周期加速

- 本地 `streamlit run app.py` 热重载，改完即见
- LLM 调试阶段先用固定的假星盘数据打样 prompt，不必每次填表单

### 5. 自动化

- 现在自动化：无（整条链路本来就是一次函数调用串起来的）
- 保持手动：部署更新（git push 触发 Streamlit Cloud 重新部署已经够了）
- 明确不做：定时任务、监控、CI——spike 不配

---

## 三、最终方案

### 架构（一条直线）

```
Streamlit 表单
   ↓
kerykeion（Swiss Ephemeris）：行星落座 / 宫位 / 上升 + SVG 星盘图
   ↓
LLM API（结构化星盘数据 + MBTI 类型 → 中文解读长文）
   ↓
结果页：星盘图 + 分节报告
   ↓（可选第二步，用户主动点击）
「再抽三张牌」→ 随机抽牌（含正逆位）→ LLM（牌面 + 已有星盘上下文 + MBTI）→ 补充解读
```

### 塔罗可选模块（v1.2 新增，响应朋友的主动请求）

- **入口**：报告页底部按钮，默认不展开——主漏斗零改动，不想用的朋友体验无变化
- **抽牌**：78 张标准韦特牌随机抽 3 张（过去/现在/未来牌阵），每张独立随机正逆位，纯 `random` 实现，零外部依赖
- **解读**：追加一次 LLM 调用，prompt 复用本次已生成的星盘上下文与 MBTI，明确要求"结合星盘与人格类型解读这三张牌"——即朋友原始诉求中的"结合在一起分析"
- **可选输入**：抽牌前一行文本框让用户写想问的事，留空则通用解读
- **实验价值**：塔罗与星盘在 API 后台是两类调用，观察期可分别统计使用率与回访构成，实验可读性不降反升
- **时间盒**：半天封顶；若周末二排期吃紧则砍，星盘主线优先
- **佐证记录**：需求来自一位朋友的产品前口头表态（弱佐证）。若上线后连提需求者本人都未使用，记为有效反面结论

### 表单字段

|字段|控件|备注|
|---|---|---|
|出生日期|日期选择器|必填|
|出生时间|时间选择器 + 「不知道出生时间」勾选框|勾选后进入降级模式|
|出生地点|城市文本输入（GeoNames 解析）|**提示用拼音或英文输入（如 Shanghai）**——GeoNames 对中文地名解析不可靠；解析失败时提示换更大的城市名|
|MBTI|16 型下拉框|必填，可加一项「不确定」则报告跳过 MBTI 交叉分析|

### 生时降级逻辑（需求 b）

- 知道时间：完整报告 = 行星落座 + 上升 + 十二宫位 + MBTI 交叉分析
- 不知道时间：按当日 12:00 计算行星落座（太阳、水金火等按天的粒度基本稳定），**报告中不呈现上升与宫位**，并在报告开头一句话说明"因出生时间未知，本报告不含上升星座与宫位分析"——诚实降级，不装全知。
- **月亮特殊处理（v1.4 新增）**：月亮一天走约 13°，约每两天半换座。生时未知时必须检查当天 0:00 与 24:00 的月亮星座是否一致：一致则正常呈现；不一致则在报告中如实说明"你出生当天月亮由 X 座进入 Y 座，因出生时间未知无法确定"，并对两种可能各做简述。装确定会毁掉"准"的体感，坦白反而显得严谨。

### LLM 解读设计

- 输入：**直接使用 kerykeion 自带的 `context_serializer`**——该模块把星盘数据转成为 LLM 优化的 XML 上下文（行星落座、上升、宫位、相位的"ground truth"），不必自己手写 JSON 化逻辑。在此基础上追加 MBTI 类型字段。
- 输出结构（在 prompt 中固定分节）：
    1. 核心性格画像（星盘与 MBTI 的共振点）
    2. 星盘与 MBTI 的张力点（两套体系矛盾的地方——这往往是读者觉得"准"的来源）
    3. 关系与沟通风格
    4. 当前阶段的一句话建议
- 语气要求写进 prompt：具体、有细节、不堆砌占星术语，术语首次出现时用一句白话解释。
- 模型任选 OpenAI / Anthropic API，按量付费；10 人用量成本预计 < $1。

### 隐私与免责（spike 级别，但要做）

- 页面底部一行声明：出生信息仅用于本次计算，**不存储、不留日志**。
- 代码层面确实不落盘：不写数据库、不打印用户输入到日志。这既是对朋友的态度，也是将来若商业化时的合规起点（你所在的魁北克 Law 25 对出生数据这类个人信息要求严格，现在养成习惯成本最低）。
- **免责声明（v1.3 新增）**：报告底部固定一句"本报告由 AI 生成，内容仅供娱乐与自我探索，不构成心理、医疗或重大决策建议"。报告会被截图转发，这句话是产品下限，也是商业化时本来就需要的。

### 密钥与成本安全（v1.3 新增，与公开仓库配套的硬性要求）

- **API key 绝不进代码**：本地放 `.streamlit/secrets.toml` 并写入 `.gitignore`，Streamlit Cloud 端在应用后台的 Secrets 配置。仓库是公开的，提交过一次 key 就等于泄露，撤都撤不干净。
- **API 账户设硬性消费上限**（建议 $10）：链接公开可转发，消费上限是防盗刷和防链接扩散的最后保险丝。
- 建仓库后第一个提交就是 `.gitignore`，先于任何代码。

### Streamlit 工程注意（v1.3 新增）

- **重跑机制是本项目最大的实现坑**：Streamlit 每次交互都从头重跑整个脚本。若不处理，用户点"抽三张牌"会把星盘计算和 30–60 秒的报告 LLM 调用原样再跑一遍——用户等两轮、你付两份钱。
- **对策**：用 `st.session_state` 缓存已生成的报告文本与星盘上下文；塔罗按钮只触发增量的抽牌与一次追加 LLM 调用。周末一拼装表单时就按这个结构写，不要等塔罗模块时再返工。

### 许可合规（v1.1 新增）

- **kerykeion 是 AGPL-3.0**：作者明确表示，直接 import 该库的项目原则上应以兼容协议开源；闭源/商用场景应改用其付费的 Astrologer API（REST 调用不受 copyleft 约束）。
- **Spike 阶段决策：仓库直接建成公开开源**。本项目无商业机密，开源即合规，顺带作为作品集。
- **商业化路径预案**：若将来过了商业化审查，切换到 Astrologer API——它提供专为 LLM 设计的 `/context/*` 端点，直接返回 AI 优化的上下文字符串，与现有架构几乎无痛替换。即"现在用库免费跑，将来切 API 解决合规"。

---

## 三点五、参考项目（开源调研结论）

|项目|借鉴什么|
|---|---|
|**gsinghjay/zodiac-engine**|与本方案最接近的成品：Kerykeion 算盘 + SVG 图 + LLM 解读（Gemini/GPT）。重点读其 InterpretationService 的 prompt 组织方式；工程结构（FastAPI 多服务）比 spike 重，不照搬|
|**g-battaglia/kerykeion** 官方文档|`context_serializer`（LLM 上下文输出）与 `ReportGenerator`（文字版报告）的用法，替代自研数据序列化|
|**catch-twenty2/AstroChart_Analysis**|它生成的"喂给 ChatGPT 的星盘数据清单"（行星、上升中天、元素分布、相位）可作为 prompt 星盘部分的模板|
|**qwq202/Aurora-MBTI**|中文 AI 解读的 prompt 与输出语感参考（其 MBTI 测试题部分已被本方案删除，不看）|
|**g-battaglia/Astrologer-API**|商业化阶段的替代数据源，现阶段只需知道它存在|

---

## 四、实施计划

### 周末一：跑通全链路（目标：本地能出一份完整报告）

0. 前置（1–2 小时）：GitHub 建**公开**仓库（AGPL 合规），**第一个提交是 `.gitignore`（含 secrets.toml），随后配置 Streamlit secrets 与 API 消费上限**；快速翻 zodiac-engine 的 LLM 解读服务源码和 kerykeion 的 `context_serializer` 文档，避免重复造轮子
1. 环境：`pip install streamlit kerykeion openai`（或 anthropic），注册 GeoNames 免费账号
2. 半天：kerykeion 打样——用自己的生日算一次，确认行星落座、上升、宫位、SVG 图、`context_serializer` 输出都拿得到；**专门用中文和拼音各测几个城市名，确认 GeoNames 解析行为并据此写兜底文案**
3. 半天：用固定的 context_serializer 输出反复调 LLM prompt 直到解读质量满意（这是本项目真正的产品工作，值得花最多时间；prompt 结构参考 AstroChart_Analysis，中文语感参考 Aurora-MBTI）
4. 半天：Streamlit 表单 + 降级逻辑 + 结果页拼装
5. 部署到 Streamlit Community Cloud（免费），手机自测

### 周末二：塔罗模块 + 打磨与发放

1. 半天封顶：塔罗可选模块（抽牌函数 + 追加 LLM 调用 + 报告页底部入口）——**排期吃紧时第一个砍**
2. 生时未知路径、地名解析失败路径的兜底文案
3. 找 1–2 个朋友当面测一遍，观察卡点（尤其地名输入）；提塔罗需求的那位朋友优先请来测
4. 修完发群，之后**不再主动提及**——验收标准要求的是无推销的自发回访

### 观测方式（无数据库版）

Streamlit Cloud 自带基础访问统计；更简单的土办法：LLM API 后台的调用次数就是使用次数。发放当天记一个基线，两周后看有没有超出首轮使用的增量调用。塔罗与星盘是两类调用，分别统计——重点看三个数：星盘生成数、塔罗使用率、回访增量里两者的构成。

---

## 五、风险清单（已定价，可接受）

|风险|影响|对策|
|---|---|---|
|Streamlit 免费实例闲置休眠，首次打开慢 30 秒|朋友以为挂了|发群时附一句"第一次打开要等半分钟"|
|地名解析失败（小城镇/拼写）|填表卡死|提示"输入最近的大城市即可，误差可忽略"|
|LLM 生成慢（长文 30–60 秒）|等待焦虑|Streamlit spinner + "正在排盘解读"文案|
|解读质量平庸，像通用星座运势|核心假设直接证伪|这不是风险，这是 spike 要的答案——质量调不上去本身就是有效结论|
|API key 泄露进公开仓库|额度被盗刷|`.gitignore` 先行 + Streamlit secrets + 账户消费上限 $10|
|链接被转发到朋友圈之外|调用量失控|消费上限兜底；调用量异常时在 Streamlit 加一个简单口令即可|
|塔罗点击触发全量重跑|双倍等待与费用|`st.session_state` 缓存报告，周末一就按此结构写|

### 终止条件（写死，防止 spike 长成项目）

- 两个周末做不完 → 停，归档
- 两周观察期内无人自发二次使用 → 停，归档
- 有真实拉力 → 回到职业审查清单，重新过"商业化"这一关（届时再谈数据源合规、支付、隐私实名等完全不同量级的问题）